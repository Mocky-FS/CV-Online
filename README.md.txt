npm init -y

pour le package.json 

npm i sass

pour l'installation sass

package.json > changer le script et mettre "sass": "sass -w scss:css"

pour mettre en route le sass en continue il faut 

npm run sass

fin